"""FastAPI / Streamlit application (FUTURE).

# Skeleton (uncomment when needed):
#
#     from fastapi import FastAPI
#     from ri.retrieval.engine import SearchService
#
#     app = FastAPI(title="RI search")
#     svc = SearchService()
#
#     @app.get("/search")
#     def search(q: str, mode: str = "classe"):
#         return svc.run(q, mode=mode)
#
# TODO: pip install fastapi uvicorn (or streamlit) before enabling.
"""
